import styled from 'styled-components'

export const Header = styled.header`

nav{
        display: flex;
        justify-content: space-between;
        padding: 0 5rem;
        height: 3.5rem;
        width: 80vw;
        margin: 0 auto;
}
`